apiVersion: v2
name: "cloudflared-tunnel"
# Since we are using this chart directly from ArgoCD, we aren't making use of the chart versioning.
version: "1.0.0"
maintainers:
  - name: GiganticMinecraft
