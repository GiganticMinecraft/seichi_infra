apiVersion: v1
kind: Service
metadata:
  name: reloader-reloader
  labels:
    app: reloader-reloader
spec:
  selector:
    app: reloader-reloader
  ports:
    - name: http
      port: 9090
      targetPort: http
