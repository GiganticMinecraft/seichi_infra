apiVersion: v1
kind: Service
metadata:
  name: argo-workflows-webhook-service
  namespace: seichi-minecraft
spec:
  selector:
    app: argo-workflows
  ports:
    - name: argo-workflows-webhook
      protocol: TCP
      port: 12000
      targetPort: 12000
