apiVersion: v1
kind: Service
metadata:
  name: mcserver--migrator
  labels:
    app: mcserver
    role: metrics
    mcserver: migrator
spec:
  type: ClusterIP
  ports:
    - name: minecraft
      port: 25565
      protocol: TCP
      targetPort: minecraft
  selector:
    mcserver: migrator
