events {
    worker_connections 32;
}

http {
    server {
        listen 80;
        
        location / {
            proxy_pass http://seichi-game-data-publisher-server:80/metrics;
        }
    }
}
