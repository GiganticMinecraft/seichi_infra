apiVersion: v1
kind: Service
metadata:
  namespace: seichi-minecraft
  name: bugsink
spec:
  type: ClusterIP
  ports:
    - name: bugsink
      port: 80
      protocol: TCP
      targetPort: 9000
  selector:
    app: bugsink
