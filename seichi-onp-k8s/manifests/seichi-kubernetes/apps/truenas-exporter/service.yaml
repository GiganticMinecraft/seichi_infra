apiVersion: v1
kind: Service
metadata:
  name: truenas-exporter
  labels:
    app: truenas-exporter
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 9814
      protocol: TCP
      targetPort: 9814
  selector:
    app: truenas-exporter
