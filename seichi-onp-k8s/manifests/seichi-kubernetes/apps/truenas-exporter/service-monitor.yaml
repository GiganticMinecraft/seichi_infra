apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: truenas-exporter
  labels:
    app: truenas-exporter
    release: prometheus
spec:
  endpoints:
    - interval: 60s
      port: metrics
  selector:
    matchLabels:
      app: truenas-exporter
