apiVersion: v1
kind: Service
metadata:
  name: bungeecord-jmx-metrics
spec:
  type: ClusterIP
  ports:
    - name: bungeecord-jmx-metrics-port
      port: 18321
      protocol: TCP
      targetPort: bungeecord-jmx-metrics-port
  selector:
    app: bungeecord
