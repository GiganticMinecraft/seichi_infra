apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-redis--redisbungee
  labels:
    app: cloudflared-redis--redisbungee
    appgroup: cloudflared
    env: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflared-redis--redisbungee
      appgroup: cloudflared
      env: prod
  template:
    metadata:
      labels:
        app: cloudflared-redis--redisbungee
        appgroup: cloudflared
        env: prod
    spec:
      containers:
      - name: cloudflared-redis--redisbungee
        image: cloudflare/cloudflared:2021.12.1
        args:
          - access
          - tcp
          - --loglevel
          - debug
          - --hostname
          - redis--redisbungee.tcp-prod-network.seichi.click
          - --url
          - 0.0.0.0:6379
---
apiVersion: v1
kind: Service
metadata:
  name: cloudflared-redis--redisbungee
spec:
  selector:
    app: cloudflared-redis--redisbungee
    appgroup: cloudflared
    env: prod
