apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cluster-wide-apps
resources:
  # cloudflared
  - ./origin-cloudflared/origin-cloudflared.yaml
  # reloader
  - github.com/stakater/Reloader/deployments/kubernetes?v0.0.104
  # descheduler
  - github.com/kubernetes-sigs/descheduler/kubernetes/cronjob?ref=v0.23.1

patchesStrategicMerge:
  - ./descheduler/patch-schedule.yaml
